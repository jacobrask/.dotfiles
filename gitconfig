[user]
    name = Jacob Rask
    email = jacob@jacobrask.net

[apply]
    whitespace = fix

[color]
    ui = auto
    diff = auto

[merge]
    tool = vimdiff

[advice]
    statusHints = false

[alias]
    pullsubs = !git submodule foreach git pull origin master
